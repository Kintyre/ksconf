[bumpversion]
current_version = 1.0.0
commit = True
message = [jinja2-markdown] Bump version: {current_version} → {new_version}

[bumpversion:file:setup.py]
search = version="{current_version}"
replace = version="{new_version}"
